native_info(){
  info_for_app clang
  info_for_app cmake
  info_for_app conan
  info_for_app ninja
}

info(){
  main_info
  native_info
  # clear color
  echo -e "\033[0m"
}

if [ -e $DEVENV_SSH_SERVER ]; then
  echo "${normal}Type ${bold}'info' ${normal}for the image information."
  # clear color
  echo -e "\033[0m"
fi
